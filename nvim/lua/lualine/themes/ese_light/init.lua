local colors = require('lualine.themes.ese_light.colors')

local theme = {
	normal = {
		c = { fg = colors.fg, bg = colors.bg },
	},
	inactive = {
		c = { fg = colors.fg, bg = colors.bg },
	},
}

return theme
