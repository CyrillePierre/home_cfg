gui:
  nerdFontsVersion: "3"
  theme:
    selectedLineBgColor:
      # - '#000000'
      - underline
git:
  log:
    showGraph: always
    showWholeGraph: true
  pagers:
    - colorArg: always
      pager: delta --paging=never
  autoForwardBranches: allBranches
